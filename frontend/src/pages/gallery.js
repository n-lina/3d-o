import React from 'react';

const Gallery = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Gallery</h1>
    </div>
  );
};

export default Gallery;